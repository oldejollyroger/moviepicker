// sw.js

// This is the simplest possible service worker to make the app installable.
self.addEventListener('fetch', (event) => {
  // We are not adding any offline logic yet, just fulfilling the requirement.
});